# دليل إعداد المصادقة في Supabase - باللغة العربية
## Supabase Authentication Setup Guide - Arabic

---

## 📋 نظرة عامة

هذا الدليل يشرح كيفية إعداد المصادقة (Authentication) في Supabase لموقع جمعية الجالية اليمنية برمنغهام.

---

## 🔐 الإعدادات المطلوبة في لوحة تحكم Supabase

### 1️⃣ تكوين عنوان الموقع الرئيسي

اذهب إلى: **Authentication → URL Configuration**

في حقل **Site URL** ضع:
```
https://yca-birmingham-websi-zmk0.bolt.host
```

---

### 2️⃣ إضافة عناوين إعادة التوجيه المصرح بها

في نفس الصفحة (**Authentication → URL Configuration**)، في قسم **Redirect URLs**، أضف العناوين التالية واحداً تلو الآخر:

**العنوان الأول:**
```
https://yca-birmingham-websi-zmk0.bolt.host/member/dashboard
```
**السبب:** لإعادة توجيه المستخدم بعد تسجيل الدخول عبر Google

**العنوان الثاني:**
```
https://yca-birmingham-websi-zmk0.bolt.host/member/login
```
**السبب:** لإعادة توجيه المستخدم بعد التحقق من البريد الإلكتروني

**العنوان الثالث:**
```
https://yca-birmingham-websi-zmk0.bolt.host
```
**السبب:** عنوان احتياطي عام

**العنوان الرابع:**
```
https://gqiwjkuddhwtaylvqqsb.supabase.co/auth/v1/callback
```
**السبب:** معالج callback الخاص بـ Supabase (ضروري لـ OAuth)

---

### 3️⃣ تفعيل المصادقة عبر البريد الإلكتروني

اذهب إلى: **Authentication → Providers → Email**

**تأكد من:**
- ✅ **Enable Email Provider:** مفعّل (ON)
- ✅ **Confirm Email:** غير مفعل (OFF) - إلا إذا كنت تريد إرسال بريد تأكيد لكل مستخدم جديد
- ✅ **Secure Email Change:** مفعّل (ON)
- ✅ **Secure Password Change:** مفعّل (ON)

**ثم اضغط على زر "Save"**

---

### 4️⃣ تكوين تسجيل الدخول عبر Google (اختياري)

اذهب إلى: **Authentication → Providers → Google**

**الخطوات:**

1. **فعّل Google Provider** (ضع المفتاح على ON)

2. **احصل على بيانات اعتماد Google OAuth:**
   - اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
   - أنشئ مشروعاً جديداً أو اختر مشروعاً موجوداً
   - من القائمة الجانبية: **APIs & Services → Credentials**
   - اضغط على **Create Credentials → OAuth Client ID**
   - اختر **Application Type: Web Application**
   - في **Authorized redirect URIs** أضف:
     ```
     https://gqiwjkuddhwtaylvqqsb.supabase.co/auth/v1/callback
     ```
   - اضغط **Create** وستحصل على **Client ID** و **Client Secret**

3. **أضف البيانات في Supabase:**
   - انسخ **Client ID** و **Client Secret** من Google Console
   - الصقهم في حقول Supabase المخصصة
   - اضغط **Save**

---

## ✅ قائمة التحقق قبل البدء

قبل أن تبدأ باختبار الموقع، تأكد من:

- [ ] ✅ تم إضافة **Site URL** بشكل صحيح
- [ ] ✅ تم إضافة جميع **Redirect URLs** الأربعة
- [ ] ✅ تم الضغط على زر **"Save changes"** بعد كل تعديل
- [ ] ✅ تم تفعيل **Email Provider**
- [ ] ✅ تم تكوين **Google OAuth** (إذا كنت تريد استخدامه)
- [ ] ✅ ملف `.env` يحتوي على البيانات الصحيحة

---

## 🧪 اختبار المصادقة

### اختبار 1: تسجيل الدخول بالبريد الإلكتروني

1. افتح الموقع واذهب إلى صفحة تسجيل الدخول:
   ```
   https://yca-birmingham-websi-zmk0.bolt.host/member/login
   ```

2. أدخل بريدك الإلكتروني وكلمة المرور

3. اضغط "تسجيل الدخول"

4. **النتيجة المتوقعة:** يجب أن يتم توجيهك إلى:
   ```
   https://yca-birmingham-websi-zmk0.bolt.host/member/dashboard
   ```

---

### اختبار 2: تسجيل الدخول عبر Google

1. في صفحة تسجيل الدخول، اضغط على زر **"التسجيل عبر جوجل"**

2. اختر حساب Google الخاص بك

3. **النتيجة المتوقعة:** يجب أن يتم توجيهك إلى:
   ```
   https://yca-birmingham-websi-zmk0.bolt.host/member/dashboard
   ```

---

### اختبار 3: إنشاء حساب جديد

1. اذهب إلى صفحة التسجيل:
   ```
   https://yca-birmingham-websi-zmk0.bolt.host/member/signup
   ```

2. أدخل بياناتك (الاسم، البريد الإلكتروني، كلمة المرور)

3. اضغط "سجّل الآن"

4. **النتيجة المتوقعة:**
   - إذا كان تأكيد البريد مفعلاً: ستصلك رسالة تأكيد على بريدك
   - إذا كان غير مفعل: سيتم توجيهك مباشرة إلى لوحة التحكم

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: رسالة خطأ "Invalid Redirect URL"

**السبب:** عناوين URL غير مضافة بشكل صحيح في Supabase

**الحل:**
1. ارجع إلى لوحة تحكم Supabase
2. تحقق من أن جميع العناوين الأربعة مضافة بشكل صحيح
3. تأكد من عدم وجود مسافات أو أخطاء إملائية
4. اضغط على زر "Save changes"
5. انتظر دقيقة أو دقيقتين لتطبيق التغييرات

---

### المشكلة 2: تسجيل الدخول عبر Google لا يعمل

**السبب:** إعدادات Google OAuth غير صحيحة

**الحل:**
1. تحقق من أن **Client ID** و **Client Secret** صحيحين
2. تأكد من أن Authorized redirect URI في Google Console هو:
   ```
   https://gqiwjkuddhwtaylvqqsb.supabase.co/auth/v1/callback
   ```
3. تأكد من تفعيل **Google+ API** في Google Cloud Console

---

### المشكلة 3: لا تصل رسائل التحقق بالبريد الإلكتروني

**السبب:** إعدادات البريد الإلكتروني غير صحيحة

**الحل:**
1. تحقق من إعدادات **Email Provider** في Supabase
2. تأكد من تفعيل **Confirm Email** إذا كنت تريد إرسال رسائل تأكيد
3. تحقق من مجلد الرسائل غير المرغوب فيها (Spam)
4. للاختبار، يمكنك تعطيل **Confirm Email** مؤقتاً

---

### المشكلة 4: المستخدم لا يتم توجيهه إلى لوحة التحكم

**السبب:** Redirect URLs غير مضافة أو غير صحيحة

**الحل:**
1. تحقق من إضافة `/member/dashboard` إلى Redirect URLs
2. تأكد من الضغط على "Save changes"
3. امسح ذاكرة التخزين المؤقت (Cache) في المتصفح
4. حاول مرة أخرى

---

## 📱 اختبار على الأجهزة المختلفة

بعد إتمام الإعداد، تأكد من اختبار المصادقة على:

- ✅ المتصفحات المختلفة (Chrome, Firefox, Safari, Edge)
- ✅ الهواتف المحمولة (Android و iOS)
- ✅ الأجهزة اللوحية (Tablets)

---

## 📊 معلومات المشروع

**اسم المشروع:** موقع جمعية الجالية اليمنية برمنغهام

**Supabase Project ID:** gqiwjkuddhwtaylvqqsb

**عنوان الموقع:** https://yca-birmingham-websi-zmk0.bolt.host

**نوع المصادقة المدعومة:**
- ✅ البريد الإلكتروني وكلمة المرور
- ✅ Google OAuth (اختياري)

---

## 📚 مصادر إضافية

للمزيد من المعلومات، راجع:

1. **دليل الإعداد الكامل (بالإنجليزية والعربية):**
   - `SUPABASE_AUTH_CONFIGURATION.md`

2. **دليل الإعداد السريع:**
   - `SUPABASE_AUTH_QUICK_SETUP.md`

3. **وثائق Supabase الرسمية:**
   - https://supabase.com/docs/guides/auth

4. **وثائق Google OAuth:**
   - https://developers.google.com/identity/protocols/oauth2

---

## ✨ نصائح إضافية

1. **احتفظ بنسخة احتياطية** من بيانات اعتماد Google OAuth في مكان آمن

2. **لا تشارك** ملف `.env` على GitHub أو أي مكان عام

3. **استخدم HTTPS دائماً** - لا تستخدم HTTP في الإنتاج

4. **فعّل المصادقة الثنائية (2FA)** على حساب Supabase الخاص بك

5. **راقب سجلات المصادقة** في لوحة تحكم Supabase بانتظام

---

## 🎯 الخطوات التالية

بعد إتمام إعداد المصادقة بنجاح، يمكنك:

1. ✅ إضافة المزيد من المستخدمين
2. ✅ تخصيص رسائل البريد الإلكتروني
3. ✅ إضافة موفري مصادقة إضافيين (Facebook, Twitter, إلخ)
4. ✅ تكوين قواعد Row Level Security (RLS) للبيانات
5. ✅ إنشاء أدوار ومستويات صلاحيات مخصصة

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل أو كانت لديك أسئلة:

- 📧 **الدعم الفني لـ Supabase:** https://supabase.com/support
- 📖 **المنتدى المجتمعي:** https://github.com/supabase/supabase/discussions
- 💬 **Discord:** https://discord.supabase.com

---

**تم آخر تحديث:** 8 فبراير 2026

**الحالة:** ✅ جاهز للاستخدام

**الإصدار:** 1.0
